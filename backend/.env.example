# ==============================================
# Step.uz Backend Environment Variables
# ==============================================

# Server Configuration
NODE_ENV=development
# Renderda BACKEND portini 10000 ga qo'yish tavsiya etiladi
PORT=10000

# Database Configuration
# Render PostgreSQL ulanish stringi (production uchun)
DATABASE_URL="postgresql://step_uz_user:erYVbhFtUqxPgNC75rVxDkCcr85JpxG6@dpg-d6pf9teuk2gs73ctgv10-a/step_uz"

# JWT Configuration
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
# Lokalda:
# FRONTEND_URL=http://localhost:3000
# Render production:
FRONTEND_URL=https://stepuz-frontend.onrender.com

# Backend public URL (masalan emaildagi linklar uchun)
BACKEND_URL=https://step-uz.onrender.com

# Email yuborish (SMTP) konfiguratsiyasi
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_FROM="Step.uz <no-reply@step.uz>"

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_DIR=uploads

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
